@model MVCForum.Website.Areas.Admin.ViewModels.LatestUsersViewModels

@if(Model.Users.Any())
{
 <ul>
 @foreach (var user in Model.Users)
 {
    <li><a href="@user.NiceUrl">@user.UserName</a></li>
 }
 </ul>
}
else
{
    <p>No data</p>
}