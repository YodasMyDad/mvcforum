@using MVCForum.Website

@if (SiteConstants.ChosenEditor == "tinymceeditor")
{
    @Html.Partial("customeditors/tinymceeditor")
}

@if (SiteConstants.ChosenEditor == "bbeditor")
{
    @Html.Partial("customeditors/bbeditor")
}

@if (SiteConstants.ChosenEditor == "markdowneditor")
{
    @Html.Partial("customeditors/markdowneditor")
}
