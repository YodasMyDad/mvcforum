@model MVCForum.Website.ViewModels.ListLatestMembersViewModel

@if(Model.Users.Any())
{
    <ul class="latestmembersjoined">
    @foreach (var user in Model.Users)
    {
        <li><a href="@user.Value">@user.Key</a></li>
    }
    </ul>
}